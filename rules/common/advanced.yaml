meta:
  category: Advanced Attack Vectors
  version: "4.0.0"
  maintainer: "Security Engineering Team"
  updated: "2026"
  scope: "PHP native 8.0-8.4 (framework-agnostic baseline)"
  compliance:
    - OWASP-Top10-2021
    - CWE-SANS-Top25
rules:
  - id: ADV-PT03
    title: "Zip Slip - ZIP extraction without output path validation"
    description: >
      ZipArchive::extractTo() or similar extraction functions are used without validating the path of each entry against the target directory. A crafted ZIP file can contain entries with path traversal (../../etc/cron.d/backdoor) that write files outside the intended extraction directory, allowing remote code execution, privilege escalation, or persistence.

    severity: critical
    confidence: high
    category: Zip Slip
    tags: [file-upload, path-traversal, zip-slip, owasp-a01, cwe-22]
    owasp: "A01:2021"
    cwe: "CWE-22"
    patterns:
      - type: regex
        target: php-files
        pattern: '(ZipArchive|zip_open|->extractTo)\s*\('
        exclude_pattern: '(realpath|str_starts_with|basename|sanitize|validate|checkEntry)'
    remediation: |
      Validate each entry before extraction. Reject the entire archive if any
      entry is suspicious:

        function safeExtract(string $zipPath, string $destDir): void
        {
            $zip     = new ZipArchive;
            $destDir = rtrim(realpath($destDir), DIRECTORY_SEPARATOR) . DIRECTORY_SEPARATOR;

            if ($zip->open($zipPath) !== true) {
                throw new RuntimeException('Failed to open ZIP');
            }

            for ($i = 0; $i < $zip->numFiles; $i++) {
                $entry    = $zip->getNameIndex($i);
                $fullPath = realpath($destDir . $entry) ?: ($destDir . $entry);

                if (!str_starts_with($fullPath, $destDir)) {
                    $zip->close();
                    throw new SecurityException("Zip Slip detected: {$entry}");
                }
            }

            $zip->extractTo($destDir);
            $zip->close();
        }
    references:
      - "https://cwe.mitre.org/data/definitions/22.html"
      - "https://snyk.io/research/zip-slip-vulnerability"

  - id: ADV-RC01
    title: "Race Condition — TOCTOU in file operations"
    description: >
      A file existence check (file_exists, is_file, is_readable) is followed by
      a file operation (fopen, file_get_contents, include) without atomic locking.
      Between the check and use, the file could be replaced by an attacker (symlink race).
    severity: high
    confidence: medium
    cvss_v3:
      score: 7.0
      vector: "CVSS:3.1/AV:L/AC:H/PR:L/UI:N/S:U/C:H/I:H/A:H"
    category: Race Condition
    tags: [race-condition, toctou, owasp-a04-2021, cwe-367]
    owasp: "A04:2021"
    cwe: "CWE-367"
    patterns:
      - type: regex-multiline
        target: php-files
        pattern: '(file_exists|is_file|is_readable|is_writable)\s*\(\s*\$\w+\s*\)\s*\)\s*\{[^}]{0,200}(file_get_contents|fopen|include|require|unlink|rename|copy)\s*\(\s*\$'
        exclude_pattern: '(flock|LOCK_EX|atomic|mutex|test|Test)'
    remediation: |
      Use atomic operations or file locking:
        $fp = fopen($path, 'r');
        if (flock($fp, LOCK_SH)) {
            $data = fread($fp, filesize($path));
            flock($fp, LOCK_UN);
        }
        fclose($fp);
    references:
      - "https://cwe.mitre.org/data/definitions/367.html"
